{
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "postinstall": "nuxt prepare",
    "start": "node .output/server/index.mjs",
    "heroku-prebuild": "echo OLES This runs before Heroku installs dependencies.",
    "heroku-postbuild": "echo OLES This runs after Heroku installs dependencies, but before Heroku prunes and caches dependencies.",
    "heroku-cleanup": "echo OLES This runs after Heroku prunes and caches dependencies."
  },
  "dependencies": {
    "ms": "^2.1.3",
    "nuxt": "^3.8.2",
    "vue": "^3.3.8",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@types/ms": "^0.7.34"
  }
}
